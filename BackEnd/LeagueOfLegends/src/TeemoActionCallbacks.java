public interface TeemoActionCallbacks {
    // Lo que hace Teemo
    void guerrillaWarfare(Screen s);
    void blindingDart(Screen s);
    void moveQuick(Screen s);
    void toxicShot(Screen s);
    void noxiousTrap(Screen s);
}
